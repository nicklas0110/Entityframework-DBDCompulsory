If you want to rollback the different stages of the database,
there will be rollback scripts ready for you to run.
Start with the latest version (v3) and then rollback as far as you want.

v1 -- rollback_initial_schema.sql
v2 -- rollback_add_categories.sql
v3 -- rollback_add_ratings.sql